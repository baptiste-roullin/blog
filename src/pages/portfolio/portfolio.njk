---
layout: base
title: "Baptiste,&NewLine; 34 ans ½,&NewLine; designer [ux]{.small-caps}"
hero:
  type: split
  image: banner.png
  alt: "échelles de note montrant mes compétences. 'Test utilisateur' est bien noté, 'design graphique' est mal noté,  'faire valider une page d'accueil du premier coup' est très bien noté. Et 'modestie' est même noté 6/5"
richPicture: true
eleventyNavigation:
  key: main
  title: Portfolio
  order: 3
---
<div class="mb-16 text-xl 2xl:text-2xl lg:mx-auto template-post-main">


  <div class="layout-wide md:mb-16 ">

  {% include 'components/hero-split.njk' %}</div>


    <div class="flex flex-col gap-12 my-20 lg:gap-16 sections ">
      <section class="flex flex-col gap-6">
        {% markdown %}
## C'est quoi un designer [ux]{.small-caps} ?
        {% endmarkdown   %}

    {% include "components/portfolio-tab.njk" %}


        {% markdown %}
## Mais encore ?

Y a en gros quatre étapes :

1. *Étudier* les utilisateurs et le contexte du service
2. Pour *imaginer* des services adaptés aux gens
3. … Les *traduire* en interfaces
4. Et *Tester* tout ça auprès des utilisateurs -- la boucle est bouclée.

## Mais pourquoi ?

Notamment parce que connaitre ses cibles et leur dédier des services adaptés, ça marche. Un contre-exemple : les [lignes de désir](https://fr.wikipedia.org/wiki/Ligne_de_d%C3%A9sir) et les vaines tentatives pour les encadrer.

!['barrière posée devant un chemin naturellement creusé à travers une pelouse'](/assets/images/ligne_désir.jpg)

## Et moi là-dedans

Je suis un designer UX polyvalent avec un certain penchant pour les défis fonctionnels, mettre en place des études utilisateur avec une méthodo aux petits oignons et mener des ateliers à gros enjeux.

Mentionnons également un certain goût pour les [systèmes complexes](/tags/systemes-complexes/), [pour utiliser mes outils à fond](/tags/prototypage) et pour faire le pont [entre théorie et pratique](/tags/sciences-cognitives/).

En bonus je fais aussi :
  - des audits en accessibilité
  - [des interventions à Épitech](http://misc.toutcequibouge.net/epitech)
  - [du code en dilettante](https://toutcequibouge.net/projets/)

Intéressé-e ?

[Contactez-moi](/contact){.cta} {.not-prose}

        {% endmarkdown %}
      </section>


        {# TOOLS #}

        <section class="tools">
      {% markdown %}
## Mes outils {.cta-heading}

​{.hidden}

        {% endmarkdown %}

        <div class="flex flex-wrap justify-start gap-10 md:justify-center md:flex-row layout-wide">
            {% for tool in tools %}
              <div class="flex flex-col justify-between gap-2 ">
                <span class="text-xl leading-5 w-min">{{tool.name}}</span>
                <figure class=""><img class="w-12 shadow-md rounded-2xl sm:w-16" src="{{tool.logo}}" alt=""></figure>
              </div>
            {% endfor %}
          </div>

      </section>

        {# PROJETS #}

      <section class="">
        {% markdown %}
## Quelques projets depuis 2018 {.cta-heading}

​{.hidden}

       {% endmarkdown %}

        <ul class="flex flex-col gap-8 m-auto md:gap-16 post-list layout-wide project-list">
          {% for ref in refs  %}
              <li class="flex-col card-base p-7 lg:p-12 ">
                <div class="w-full m-0 block-heading-split">
                  <h3 class="cta-heading">{{ref.title}}</h3>
                  <img src="{{ref.logo}}" alt="{{ref.name}}">
                </div>

                {# DÉTAILS #}
                <div class="flex flex-col justify-between gap-6 md:grid lg:gap-8 details ">
                  <div class="flex flex-col gap-5 lg:gap-7">
                    {% if ref.description %}
                    <dl>
                      <dt>C'est quoi</dt>
                      <dd>{{ref.description | markdownifyBlock | safe}}</dd>
                    </dl>
                    {% endif %}


                    {% if ref.durée %}
                    <dl>
                      <dt>Durée</dt>
                      <dd>{{ref.durée if ref.durée}}</dd>
                    </dl>
                    {% endif %}

                    {% if ref.tasks %}
                    <dl>
                      <dt>Ce que j'ai fait</dt>
                      <dd>
                          <ul class="inline tagpills">
                              {% for task in ref.tasks %}
                                  <li class="inline-block my-auto">
                                    {{ task }}
                                    {% include "components/separator.njk" %}
                                  </li>
                              {% endfor %}
                          </ul>
                      </dd>
                    </dl>
                    {% endif %}

                    <div class="not-prose">
                      {% if  ref.CTA %}
                      <a href="{{ref.CTALink}}" class="w-3/5 mx-auto cta">{{ref.CTA}}</a>
                      {%else %}
                      <div class="w-3/5 mx-auto text-gray-800 bg-gray-300 hover:bg-gray-400 cta">En cours ou confidentiel</div>
                      {% endif %}
                    </div>

                  </div>
                  <div class="flex flex-row flex-wrap gap-3 m-auto rich-picture ">
                     {% for photo in ref.photos   %}
                        <figure class=""><img class="w-auto shadow-md"  src="{{photo.url}}" alt="{{photo.alt}}"></figure>
                      {% endfor %}

                    </div>
                </div>
              </li>

        {% endfor %}
        </ul>
      </section>


      {# CLIENTS #}

      <section class="clients">
          {% markdown %}
## Clients principaux {.cta-heading}

​{.hidden}

          {% endmarkdown %}

        <div class="flex-col items-start p-7 lg:p-12 card-base hover:shadow layout-wide">
          <ul class="flex flex-col items-start gap-8 " >
          {% for client in clients %}

            <li class="flex flex-col w-full gap-4 m-0 list-none md:flex-row md:gap-7">
              <span class="text-xl font-medium list-none md:text-right text-accent3 sector basis-1/4 shrink-0 ">{{client.sector}}</span>
              <ul class="flex flex-row flex-wrap m-0 md:items-center gap-y-2 gap-x-2 xl:gap-x-3 " >
                    {% for item in client.list %}
                <li class="m-0 text-xl list-none ">
                  {{item}}
                </li>
                {% if not loop.last %}
                <span aria-hidden="true" class="text-xl " >~</span>
                {% endif %}
                    {% endfor %}
              </ul>
            </li>
          {% endfor %}
          </ul>
        </div>
      </section>
    </div>
  </div>
</div>



{#


wireframe #1
Un outil de gestion des plannings et activités sur les plateformes pétrolières. Enjeux :
- Fonctionnement en offline
- Équilibre entre améliorations de l'archi de l'information et respect des habitudes des utilisateurs (qui avaient l'habitude d'un tableau Excel avec une certaine mise en page)
- Manipulation de données (lignes de tableau pouvant être éditées, ajoutées, supprimées, etc.).

wireframe #2
Un outil d'aide à la décision pour des commerciaux, afin qu'ils proposent la bonne machine de test médical en fonction des informations recueillies sur le prospect. Enjeux :
- Faire comprendre aux utilisateurs l'impact des données saisies, avec une visualisation en temps réel de l'évolution du ranking des machines.
- Concevoir un outil à la fois simple (possibilité d'enchainer les étapes fluidement façon Typeform) et souple (possibilité de faire les étapes dans le désordre, l'utilisateur pouvant recueillir des infos sur plusieurs jours).
- Degré d'expertise variable entre les utilisateurs, donc besoin de guider (infobulle, explication du jargon…) sans trop alourdir.

 #}